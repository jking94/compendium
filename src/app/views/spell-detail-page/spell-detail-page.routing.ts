import { SpellDetailPageComponent } from './spell-detail-page.component';
import { Routes } from '@angular/router';

export const spellDetailPageRoutes : Routes = [
    {
        path: '',
        component: SpellDetailPageComponent
    }
];